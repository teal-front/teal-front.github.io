devTool:
1.persist: 
Right click in the console to reveal a menu with “"Preserve Log upon Navigation” as an option.
2.changing frames:
 cd(window.frames[""]);
3.copy($0)  copy the node's html
4.getEventListeners(object)
5.monitorEvents(object[, events])
IE:
1.html tab 
属性列直接写style属性
2.css的数值可以直接调大小